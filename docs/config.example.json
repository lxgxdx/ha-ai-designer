{
  "_note": "EXAMPLE config template. The real config.json (with HA token + LLM key) is gitignored. Copy this file to data/config.json and fill in the values.",
  "ha": {
    "baseUrl": "http://<your-ha-ip>:8123",
    "token": "<long-lived-access-token>"
  },
  "llm": {
    "provider": "minimax",
    "baseUrl": "https://api.minimaxi.com/v1",
    "apiKey": "<your-minimax-api-key>",
    "model": "MiniMax-Text-01"
  }
}
