{
  "name": "ha-ai-designer",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first AI designer for Home Assistant Lovelace dashboards",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "tools-dev": "pnpm --filter @ha-designer/tools-dev run dev",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "guard": "pnpm -r --if-present guard"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
